# kd-callog — 通話記録システム

Galaxyの自動通話録音をGoogle Driveへ同期し、Gemini APIで文字起こしして
スプレッドシートに蓄積、PWAから検索・閲覧する。ランニングコスト0円。

## 構成

```
Galaxy S10 (Android 12)
  └ 電話アプリの自動録音 → SDカード /storage/XXXX-XXXX/Recordings/Call/
      ファイル名: 通話記録 <連絡先名 or 電話番号>_yymmdd_hhmmss.m4a
  └ FolderSync（無料版・片方向アップロード・15分毎）
      → Google Drive /通話記録システム/受信/
GAS（5分毎トリガー）
  └ 受信フォルダを新しい順に処理（直近優先・過去分は空き枠で消化）→ Gemini APIで文字起こし＋要約
  └ スプレッドシート「通話記録DB」に追記 → 音声は「処理済み」へ移動
  └ 毎日3時：90日超の音声を自動削除（テキストは残す）
  └ doGet：JSONP対応の検索API
PWA（index.html・GitHub Pages）
  └ 一覧（日付・時刻・名前・要約）→ タップで全文＋録音再生
```

## ファイル

| パス | 役割 |
|---|---|
| `index.html` | 検索PWA。単一ファイル。設定はlocalStorageに保存 |
| `gas/code.gs` | GAS本体。**このリポジトリ版はAPIキー空欄**。実物はGASエディタ側にのみ存在 |

## スプレッドシート「通話記録DB」

- **通話記録**：日時／名前／電話番号／通話時間(目安)／要約／全文／音声リンク／ファイル名／状態
- **顧客マスタ**：名前／会社名／電話番号／メモ（番号⇔名前の補完に使用）
  - 未登録番号の通話で相手が名乗っていた場合、メモ「自動追加 日付 要確認」付きで
    候補行が自動追加される（間違いはシート上で修正すれば以後その表記が使われる）
- **用語辞書**：誤（聞き間違い）／正（正しい表記）
  - AIへのプロンプトヒント＋文字起こし後の自動置換の両方に使う
  - 長い語から先に置換する（`川口電気→河口電機` を `川口→河口` より先に）

## GASの主な関数

| 関数 | 用途 |
|---|---|
| `setup()` | 初回のみ。フォルダ・シート・トリガーを作成 |
| `processNewRecordings()` | 5分毎トリガー本体 |
| `testApiKey()` | APIキーと認証方式の疎通確認 |
| `retryErrors()` | エラーフォルダの音声を受信へ戻し、エラー行を削除して再処理 |
| `fixExisting()` | 既存レコードに用語辞書を一括適用 |
| `fillNamesFromMaster()` | 名前が空の過去行を顧客マスタから一括補完 |
| `cleanupOldAudio()` | 日次。古い音声を削除しリンクを消す |

## 検索API（doGet）

```
GET <webapp>/exec?token=<API_TOKEN>[&q=][&name=][&tel=][&from=][&to=][&id=][&full=1][&limit=][&callback=]
```
- `callback` 指定でJSONP（CORS制約を回避するためPWAは常にこちらを使用）
- `full=1` 以外は全文を先頭120字に切り詰め
- token不一致は `{"error":"unauthorized"}`

## 実装上の注意（ハマりどころ）

- **CORS**：GAS webappへのfetchはブラウザに弾かれる。JSONP必須
- **再デプロイ**：コード変更後は「デプロイを管理→編集→バージョン**新バージョン**→デプロイ」。
  バージョンを据え置くと旧コードが動き続ける
- **APIキー形式**：新形式（`AQ.`で始まる）。ヘッダー `x-goog-api-key` と `?key=` の
  両方を試して成功した方を ScriptProperties に記憶する実装になっている
- **Geminiの応答**：JSONの後ろに余計な文字が付くことがある。`parseResult()` で
  4段階（そのまま→括弧の対応を取って切り出し→正規表現→生テキスト）に救済している
- **同期の削除設定**：FolderSyncの「削除を同期」は必ずOFF。
  GASが受信→処理済みへ移動するため、ONだと端末の録音原本が消える
- **音声サイズ**：約1MB/分。18MB以下（`MAX_INLINE_MB`）はリクエストに直接埋め込み、
  18MB超はGemini Files API経由（アップロード→ACTIVE待ち→file_uriで文字起こし→即削除）。
  45MB超（`MAX_FILE_MB`・UrlFetchAppの上限対策）のみスキップ。
  大容量は処理に数分かかるため、トリガー実行の残り時間が少ない時は次回に回す

## 今後やりたいこと（未実装）

- 話者A/Bのどちらが自分かの判定
- 案件ごとのタグ付け・他ツール（KD見積・GenCan）との突合
